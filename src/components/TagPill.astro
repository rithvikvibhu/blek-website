---
interface Props {
  tag: string;
  className?: string;
}
const { tag, className = '' } = Astro.props;
---
<span class={`inline-block px-2.5 py-1 text-xs font-medium rounded-full border bg-slate-100 dark:bg-slate-800/50 text-slate-600 dark:text-slate-300 border-slate-200 dark:border-slate-700 ${className}`}>
  {tag}
</span>

